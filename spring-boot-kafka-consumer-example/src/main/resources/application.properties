#Local MongoDB config
spring.data.mongodb.database=provider_db
spring.data.mongodb.port=27017
spring.data.mongodb.host=localhost

# App config
server.port=8081
spring.application.name=providers-data
server.servlet.context-path=/providers

bootstrap.servers=localhost:9092
group.id=group-1

topic.id=My_Topic